<UserControl x:Class="GreenManager___WPF.Views.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenManager___WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="📦 Voorraad &amp; Materialen" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

		<DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Materials}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeight="35">

			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>

				<DataGridTextColumn Header="Artikelnaam" Binding="{Binding Name}" Width="*"/>

				<DataGridTextColumn Header="Voorraad" Binding="{Binding StockQuantity}" Width="100"/>

				<DataGridTextColumn Header="Eenheid" Binding="{Binding Unit}" Width="100"/>

				<DataGridTextColumn Header="Inkoopprijs" Binding="{Binding PurchasePrice, StringFormat={}{0:C}}" Width="120"/>

			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
