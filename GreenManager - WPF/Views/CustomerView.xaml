<UserControl x:Class="GreenManager___WPF.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Klantenbeheer" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
			<Button Content="+ Nieuwe Klant" Command="{Binding OpenAddWindowCommand}" 
            Padding="15,10" Background="#2980B9" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>

			<Button Content="✏️ Bewerken" Command="{Binding EditCustomerCommand}" 
            Padding="15,10" Background="#F39C12" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>

			<Button Content="🗑️ Verwijderen" Command="{Binding DeleteCustomerCommand}" 
            Padding="15,10" Background="#E74C3C" Foreground="White" FontWeight="Bold"/>
		</StackPanel>

		<DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Customers}" 
				  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="True" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All">
		</DataGrid>
	</Grid>
</UserControl>