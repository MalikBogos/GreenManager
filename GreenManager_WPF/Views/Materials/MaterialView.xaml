<UserControl x:Class="GreenManager___WPF.Views.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenManager___WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Style="{StaticResource HeaderStyle}" Text="Materiaalbeheer"/>

		<!-- Knoppenbalk -->
		<StackPanel Grid.Row="1" Style="{StaticResource StackPanelStyle}">
			<Button Style="{StaticResource AddButtonStyle}" Content="Nieuw Materiaal" Command="{Binding OpenAddWindowCommand}"/>

			<Button Style="{StaticResource EditButtonStyle}" Content="Bewerk Materiaal" Command="{Binding EditMaterialCommand}"/>

			<Button Style="{StaticResource DeleteButtonStyle}" Content="Verwijder Materiaal" Command="{Binding SoftDeleteMaterialCommand}"/>
		</StackPanel>

		<!-- Tabel met Materiaal -->
		<DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Materials}" 
                  SelectedItem="{Binding SelectedMaterial}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeight="35">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Naam" Binding="{Binding Name}" Width="*"/>
				<DataGridTextColumn Header="Beschrijving" Binding="{Binding Description}" Width="220"/>
				<DataGridTextColumn Header="Voorraad" Binding="{Binding StockQuantity}" Width="60"/>
				<DataGridTextColumn Header="Eenheid" Binding="{Binding Unit}" Width="55"/>
				<DataGridTextColumn Header="Inkoopprijs" Binding="{Binding PurchasePrice, StringFormat={}{0:C}}" Width="70"/>
				<DataGridTextColumn Header="Notities" Binding="{Binding Notes}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
