<UserControl x:Class="GreenManager_WPF.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Margin="0,0,0,15">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBlock Grid.Column="0" Style="{StaticResource HeaderStyle}" Text="Klantenbeheer"/>

			<StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
				<TextBlock Text="Zoeken:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="LightSteelBlue" Margin="0,0,15,0"/>
				<TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Width="250" Padding="8" BorderThickness="1" BorderBrush="#DCDDE1" VerticalContentAlignment="Center"/>
			</StackPanel>
		</Grid>
		
		<!-- Knoppenbalk -->
		<StackPanel Grid.Row="1" Style="{StaticResource StackPanelStyle}">
			<Button Style="{StaticResource AddButtonStyle}" Content="Nieuwe Klant" Command="{Binding OpenAddWindowCommand}"/>

			<Button Style="{StaticResource EditButtonStyle}" Content="Bewerk Klant" Command="{Binding EditCustomerCommand}"/>

			<Button Style="{StaticResource DeleteButtonStyle}" Content="Verwijder Klant" Command="{Binding SoftDeleteCustomerCommand}"/>
		</StackPanel>

		<!-- Tabel met Klanten -->
		<DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Customers}" 
				  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="false" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All">

			<DataGrid.Columns>
				<DataGridTextColumn Header="Voornaam" Binding="{Binding FirstName}" Width="Auto"/>
				<DataGridTextColumn Header="Achternaam" Binding="{Binding LastName}" Width="Auto"/>
				<DataGridTextColumn Header="Bedrijfsnaam" Binding="{Binding CompanyName}" Width="*"/>
				<DataGridTextColumn Header="BTW-Nummer" Binding="{Binding VATNumber}" Width="Auto"/>
				<DataGridTextColumn Header="Notities" Binding="{Binding Notes}" Width="*"/>
				<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
				<DataGridTextColumn Header="Telefoonnummer" Binding="{Binding PhoneNumber}" Width="Auto"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>