<UserControl x:Class="GreenManager_WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

	<Grid Margin="30">

		<!-- GAST BERICHT (Alleen tonen als rol Guest is) -->
		<Border Background="MistyRose" BorderBrush="IndianRed" BorderThickness="1" CornerRadius="8" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Center">
			<Border.Style>
				<Style TargetType="Border">
					<Setter Property="Visibility" Value="Collapsed"/>
					<Style.Triggers>
						<DataTrigger Binding="{Binding DataContext.CurrentUserRole, RelativeSource={RelativeSource AncestorType=Window}}" Value="Guest">
							<Setter Property="Visibility" Value="Visible"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Border.Style>
			<StackPanel>
				<TextBlock Text="Toegang Beperkt" FontSize="24" FontWeight="Bold" Foreground="DarkRed" HorizontalAlignment="Center" Margin="0,0,0,15"/>
				<TextBlock Text="Je moet een werknemersrol hebben om de applicatie te kunnen gebruiken met alle functionaliteiten." 
                           FontSize="16" Foreground="DarkRed" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="400" FontWeight="Normal"/>
			</StackPanel>
		</Border>

		<Grid>
			<Grid.Style>
				<Style TargetType="Grid">
					<Setter Property="Visibility" Value="Visible"/>
					<Style.Triggers>
						<DataTrigger Binding="{Binding DataContext.CurrentUserRole, RelativeSource={RelativeSource AncestorType=Window}}" Value="Guest">
							<Setter Property="Visibility" Value="Collapsed"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Grid.Style>

			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0" Margin="0,0,0,30">
				<TextBlock Text="Welkom bij GreenManager" Style="{StaticResource HeaderStyle}" Margin="0,0,0,5"/>
				<TextBlock Text="Hier is een overzicht van hoe je bedrijf er vandaag voor staat." Foreground="Gray" FontWeight="Normal"/>
			</StackPanel>

			<Grid Grid.Row="1" Margin="0,0,0,30">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="20"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="20"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<Border Grid.Column="0" Background="White" CornerRadius="8" Padding="20" BorderBrush="LightGray" BorderThickness="1">
					<StackPanel>
						<TextBlock Text="Actieve Klanten" Foreground="Gray" Margin="0,0,0,10"/>
						<TextBlock Text="{Binding TotalCustomers}" FontSize="36" FontWeight="Bold" Foreground="SteelBlue"/>
					</StackPanel>
				</Border>

				<Border Grid.Column="2" Background="White" CornerRadius="8" Padding="20" BorderBrush="LightGray" BorderThickness="1">
					<StackPanel>
						<TextBlock Text="Werknemers" Foreground="Gray" Margin="0,0,0,10"/>
						<TextBlock Text="{Binding TotalEmployees}" FontSize="36" FontWeight="Bold" Foreground="MediumSeaGreen"/>
					</StackPanel>
				</Border>

				<Border Grid.Column="4" Background="White" CornerRadius="8" Padding="20" BorderBrush="LightGray" BorderThickness="1">
					<StackPanel>
						<TextBlock Text="Lopende Projecten" Foreground="Gray" Margin="0,0,0,10"/>
						<TextBlock Text="{Binding TotalActiveProjects}" FontSize="36" FontWeight="Bold" Foreground="DarkOrange"/>
					</StackPanel>
				</Border>
			</Grid>

			<TextBlock Grid.Row="2" Text="Aankomende en Actieve Projecten" FontSize="20" Foreground="MidnightBlue" Margin="0,0,0,15"/>

			<!-- Tabel -->
			<DataGrid Grid.Row="3" ItemsSource="{Binding ActiveProjects}" AutoGenerateColumns="False" IsReadOnly="True" 
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="LightGray" 
                      Background="White" RowHeight="40">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
					<DataGridTextColumn Header="Projectnaam" Binding="{Binding Name}" Width="250"/>
					<DataGridTextColumn Header="Klant" Binding="{Binding Customer.LastName}" Width="200"/>
					<DataGridTextColumn Header="Startdatum" Binding="{Binding StartDate, StringFormat=dd-MM-yyyy}" Width="150"/>
				</DataGrid.Columns>
			</DataGrid>
		</Grid>
	</Grid>
</UserControl>