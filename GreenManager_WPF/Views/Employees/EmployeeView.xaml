<UserControl x:Class="GreenManager_WPF.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenManager_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
			 Background="White">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Style="{StaticResource HeaderStyle}" Text="Werknemersbeheer"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Style="{StaticResource AddButtonStyle}" Content="Nieuwe Werknemer" Command="{Binding OpenAddWindowCommand}"/>
			
            <Button Style="{StaticResource EditButtonStyle}" Content="Bewerk Werknemer" Command="{Binding EditEmployeeCommand}"/>

			<Button Style="{StaticResource DeleteButtonStyle}" Content="Verwijder Werknemer" Command="{Binding DeleteEmployeeCommand}"/>
		</StackPanel>

		<DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Employees}" 
                  SelectedItem="{Binding SelectedEmployee}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
				  HorizontalScrollBarVisibility="Auto"
                  RowHeight="35">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Personeelsnummer" Binding="{Binding EmployeeNumber}" Width="Auto"/>
				<DataGridTextColumn Header="Voornaam" Binding="{Binding User.FirstName}" Width="Auto"/>
				<DataGridTextColumn Header="Achternaam" Binding="{Binding User.LastName}" Width="Auto"/>
				<DataGridTextColumn Header="Telefoonnummer" Binding="{Binding User.PhoneNumber}" Width="Auto"/>
				<DataGridTextColumn Header="Functie" Binding="{Binding JobTitle}" Width="Auto"/>
				<DataGridTextColumn Header="Uurloon" Binding="{Binding HourlyWage, StringFormat={}{0:C}}" Width="Auto"/>
				<DataGridTextColumn Header="Straat" Binding="{Binding Street}" Width="Auto"/>
                <DataGridTextColumn Header="Postcode" Binding="{Binding PostalCode}" Width="120"/>
                <DataGridTextColumn Header="Stad" Binding="{Binding City}" Width="120"/>
                <DataGridTextColumn Header="Notities" Binding="{Binding Notes}" Width="120"/>
            </DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>