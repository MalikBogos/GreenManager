<UserControl x:Class="GreenManager___WPF.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenManager___WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Style="{StaticResource HeaderStyle}" Text="Projectenbeheer"/>

		<!-- Knoppenbalk -->
		<StackPanel Grid.Row="1" Style="{StaticResource StackPanelStyle}">
			<Button Style="{StaticResource AddButtonStyle}" Content="Nieuw Project" Command="{Binding OpenAddProjectWindowCommand}"/>

			<Button Style="{StaticResource EditButtonStyle}" Content="Bewerk Project" Command="{Binding EditProjectCommand}"/>

			<Button Style="{StaticResource DeleteButtonStyle}" Content="Verwijder Project" Command="{Binding SoftDeleteProjectCommand}"/>
		</StackPanel>

		<!-- Tabel met projecten -->
		<DataGrid Grid.Row="2"
                  ItemsSource="{Binding Projects}"
                  SelectedItem="{Binding SelectedProject}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeight="35">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Projectnaam" Binding="{Binding Name}" Width="250"/>
				<DataGridTextColumn Header="Klant" Binding="{Binding Customer.LastName}" Width="150"/>
				<DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
				<DataGridTextColumn Header="Startdatum" Binding="{Binding StartDate, StringFormat=dd-MM-yyyy}" Width="120"/>
				<DataGridTextColumn Header="Budget / Offertebedrag" Binding="{Binding Budget, StringFormat={}{0:C}}" Width="150"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
